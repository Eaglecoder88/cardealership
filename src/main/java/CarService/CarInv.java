package CarService;

import java.time.temporal.ChronoUnit;
import java.util.List;

public class CarInv {
	

}

public List<Listing> soldvehicleList() {
	return  soldvehicleList();
}

public void removeVehicle(int vinNumber) {
	for(Listing listing)
}

public void saveVehicleList(Listing addCar) {
	activeList.add(addcar);
}

public void removeVehicleList(int vinNumber) {
	soldList.add((searchVinr(vin)));
	activeList.remove((searchVinNumber(vin)));
	}

public List<Listing> listAll() {
	return activeList;
}

publci int calcListingAge(LocalDate dateListed) {
	int listingAge = (in) ChronoUnit.DAYSbetween(date.Listed, LocalDate.now())
			return listingAge;
}

public Listing findByStockNumber(int stockNumber() {
	
}